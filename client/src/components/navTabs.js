// Home, Dashboard, Resume, FAQs, About, Login, Sign Up
import React from 'react';

// object destructuring assignment to pull our variables from the props object then assign variable names
function NavTabs({ currentPage, handlePageChange }) {
    return (
        <h1>TEST</h1>
    )
}

export default NavTabs;
